// Tiny in-tree dependency, ESM-shape. Real npm packages have shapes
// like this: stateless utility module with default + named export.
let counter = 0;
export function nextId() {
    counter += 1;
    return "todo-" + counter + "-" + crypto.randomUUID().slice(0, 8);
}
export default nextId;
