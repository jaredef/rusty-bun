# Rust
target/
Cargo.lock.bak
**/*.rs.bk
*.pdb

# Zig
zig-cache/
zig-out/

# Build artifacts
build/
out/
dist/
*.o
*.so
*.dylib
*.dll
*.a
*.exe

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Local environment
.env
.env.local
*.log
.direnv/

# Local cargo config — redirects target/ to an out-of-tree drive on this
# host (T7 mounted at /media/jaredef/T7). Machine-specific; not portable.
/.cargo/

# Translation pipeline scratch
phase-a-scratch/
phase-b-scratch/
*.translation.tmp

# Per-host setup notes (paths, mounts, throttling — machine-specific)
LOCAL-SETUP.md
